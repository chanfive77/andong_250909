import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mainText: {
    fontSize: 20,
    fontWeight: "bold",
    color: "blue",
    borderWidth: 1,
    borderColor: "blue",
    padding: 10,
    margin: 10,
    borderRadius: 10
  }
});
